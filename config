from pydantic_settings import BaseSettings


class Settings(BaseSettings):

    DATABASE_URL:str = (
        "postgresql://postgres:password@localhost/gentherapy"
    )

    MONGO_URL:str = (
        "mongodb://localhost:27017"
    )

    REDIS_URL:str = (
        "redis://localhost:6379"
    )


    SECRET_KEY:str="BAREKAT_SECRET_KEY"

    ALGORITHM:str="HS256"

    ACCESS_TOKEN_EXPIRE_MINUTES:int=60


settings = Settings()
